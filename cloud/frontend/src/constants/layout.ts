/** Viewport width (px) below which the app shell uses the mobile nav drawer. */
export const MOBILE_BREAKPOINT = 992

/** Viewport width (px) below which v-data-table uses Vuetify's mobile card rows. */
export const TABLE_MOBILE_BREAKPOINT = MOBILE_BREAKPOINT
