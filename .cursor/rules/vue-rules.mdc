---
description: "Cursor rules for Vue 3 development with Composition API integration."
globs: **/*
alwaysApply: false
---
// Vue 3 Composition API .cursorrules

// Vue 3 Composition API best practices

const vue3CompositionApiBestPractices = [
  "Use setup() function for component logic",
  "Utilize ref and reactive for reactive state",
  "Implement computed properties with computed()",
  "Use watch and watchEffect for side effects",
  "Implement lifecycle hooks with onMounted, onUpdated, etc.",
  "Utilize provide/inject for dependency injection",
];

// Folder structure

const folderStructure = `
src/
  components/
  composables/
  views/
  router/
  store/
  assets/
  App.vue
  main.ts
`;

// Additional instructions

const additionalInstructions = `
1. Cloud frontend uses TypeScript: <script setup lang="ts">, typed defineProps/emits/models
2. API calls use @/api (openapi-fetch client + apiJson<T> for dynamic paths)
3. Vuetify data-table slots: type slot props via @/types/vuetify (no implicit any)
4. Implement proper error handling with catch (e: unknown)
5. Follow Vue 3 style guide and naming conventions
6. Regenerate OpenAPI types when backend schemas change (see AGENTS.md)
`;