{
  "name": "order-platform-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:cloud": "playwright test --project=cloud",
    "test:pi": "playwright test --project=pi"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
