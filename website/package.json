{
  "name": "vendiqo-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "markdown-it": "^14.2.0"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
