# Copy to keystore.properties and fill in values. Do not commit keystore.properties.
storeFile=../order-platform-release.jks
storePassword=your-store-password
keyAlias=release
keyPassword=your-key-password
